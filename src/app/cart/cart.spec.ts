describe('Cart', () => {
  xit('🚧 should add items', () => {
    // const sandwich = createItem('Double Beurre', 6);
    // const burger = createItem('Burger', 12);
    // @todo create cart
    // @todo cart.addItem(sandwich)
    // @todo cart.addItem(burger)
    // @todo check cart.getItems() equals [sandwich, burger]
  });
});
